/**
 * **sample**
 *
 * samples a random number and sees if it is within the
 * sampling rate, if so it returns `all` otherwise `none`
 */
export declare function sample(samplingRate?: number): "all" | "none";
